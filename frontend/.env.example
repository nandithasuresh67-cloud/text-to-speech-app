# Leave empty for local development (Vite proxy handles /api and /audio).
# Set to your deployed backend's origin in production, e.g.:
# VITE_API_BASE_URL=https://text-to-speech-backend.onrender.com
VITE_API_BASE_URL=
